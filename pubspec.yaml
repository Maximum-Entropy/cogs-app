name: cogs
description: Cross-platform competitive cognitive task app. Suitable for personalized brain training or party game format.
version: 1.0.0
# repository: https://github.com/Maximum-Entropy/cogs-app

environment:
  sdk: ^3.4.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
